{
  "license": "ISC",
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "test": "anchor test",
    "test:bankrun": "anchor build && yarn ts-mocha -p ./tsconfig.json -t 1000000 tests/solana_business_card_bankrun.ts",
    "test:traditional": "anchor test",
    "deploy": "ts-node migrations/deploy.ts",
    "deploy:local": "SOLANA_ENV=localnet yarn deploy",
    "deploy:devnet": "SOLANA_ENV=devnet yarn deploy", 
    "deploy:testnet": "SOLANA_ENV=testnet yarn deploy",
    "deploy:mainnet": "SOLANA_ENV=mainnet-beta yarn deploy",
    "deploy:auto-cleanup": "AUTO_CLEANUP=true yarn deploy",
    "setup-env": "ts-node scripts/setup-env.ts",
    "env": "yarn setup-env"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "anchor-bankrun": "^0.5.0",
    "solana-bankrun": "^0.4.0"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^5.2.1",
    "@types/mocha": "^10.0.0",
    "@types/node": "^24.0.0",
    "chai": "^5.2.0",
    "mocha": "^11.0.0",
    "prettier": "^3.6.2",
    "ts-mocha": "^11.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.3"
  }
}
